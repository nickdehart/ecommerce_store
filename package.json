{
  "name": "ecommerce_store",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "echo \"No test specified\" && exit 0",
    "dev": "SET NODE_ENV=sandbox && node ./server",
    "build": "next build",
    "start": "SET NODE_ENV=production && node ./server",
    "deploy-staging": "NODE_ENV=staging next build && NODE_ENV=staging node ./server",
    "deploy-production": "NODE_ENV=production next build && NODE_ENV=production node ./server"
  },
  "dependencies": {
    "@paypal/checkout-server-sdk": "^1.0.2",
    "@zeit/next-css": "^1.0.1",
    "bootstrap": "^4.4.1",
    "dotenv": "^8.2.0",
    "mongo-sanitize": "^1.0.1",
    "mongodb": "^3.4.0",
    "next": "9.1.5",
    "next-ga": "^2.3.4",
    "next-routes": "^1.4.2",
    "react": "16.12.0",
    "react-async-script-loader": "^0.3.0",
    "react-bootstrap": "^1.0.0-beta.16",
    "react-dom": "16.12.0",
    "react-paypal-button-v2": "^2.6.1",
    "react-square-payment-form": "^0.5.0",
    "sweetalert2": "^9.5.3",
    "sweetalert2-react-content": "^2.0.2",
    "xml-js": "^1.6.11"
  },
  "devDependencies": {
    "@fortawesome/fontawesome-free": "^5.11.2"
  }
}
